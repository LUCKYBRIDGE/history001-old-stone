# AGENTS.md

이 저장소는 초등학생용 **구석기 역사 체험 웹게임** 프로젝트의 단일 기준점(Single Source of Truth)이다.

## 1. 새 ChatGPT 작업 세션의 시작 순서

1. `AGENTS.md`
2. `PROJECT_STATUS.md`
3. `docs/00_DEVELOPMENT_WORKFLOW.md`
4. `handoff/CURRENT_HANDOFF.md`
5. 이번 작업과 직접 관련된 `docs/` 문서
6. 실제 개발이면 관련 코드와 테스트

과거 채팅 기억보다 GitHub 최신 문서를 우선한다.

## 2. 문서 위계

1. `docs/01_PROJECT_CORE.md` — 교육·체험 헌법
2. `docs/02_EXPERIENCE_STRUCTURE.md` — 전체 경험 구조
3. `docs/07_IMMERSION_NARRATIVE_BIBLE.md` — 전 역할 몰입·내러티브 공통 기준
4. `docs/05_ROLE_EXPERIENCE_MAP.md` — 역할 경계·균형
5. 역할별 `*_STORY.md`
6. 역할별 immersion redesign / script
7. 역할별 `*_PLAYFLOW.md`
8. `docs/06_TECH_BLUEPRINT.md` 및 구현 문서

하위 문서나 코드가 상위 원칙과 충돌하면 임의로 정당화하지 말고 충돌을 명시한다.

## 3. 프로젝트 최상위 원칙

- 학생에게 구석기 시대를 설명하는 웹이 아니라 **그 생활 속에 잠시 들어가 살아보게 하는 웹게임**을 만든다.
- 학생이 `게임을 조작하는 나`보다 `이 공동체의 한 사람인 나`로 생각하게 만드는 **역할 빙의(Role Embodiment)**를 핵심 학습 장치로 취급한다.
- 몰입은 최종 이미지·사운드 단계에 붙이는 장식이 아니다. STORY, PLAYFLOW, 상호작용, UI, 텍스트, 시간·공간 연속성부터 검증한다.
- 설명보다 **상황 → 행동 → 결과/감각 → 짧은 해석 → 개념화** 순서를 우선한다.
- 시작 20~30초 안에 장소, 사람, 생활 문제, 첫 의미 있는 행동이 드러나야 한다.
- 학생용 화면에는 Stage 번호, Vertical Slice, RoleEntry, 내부 state/signal 같은 개발 메타 정보를 노출하지 않는다. 필요하면 debug/development mode로 분리한다.
- 사냥·채집·머무는 사람은 주역/보조역이 아니라 같은 공동체의 하루를 보여주는 동등한 세 관점이다.
- 먼저 개발된 역할의 플레이 문법을 다른 역할에 자동 복제하지 않는다.
- 프로젝트의 핵심 단위는 세 개의 미니게임이 아니라 **하나의 공동체가 보내는 하루**다.
- 실패는 `GAME OVER`가 아니라 당시 생활의 불확실성을 경험하는 정상적인 결과다.
- 점수, HP, EXP, 랭킹, 전투 중심 구조를 임의로 추가하지 않는다.
- 위험은 공포·전투의 쾌감보다 자연 속 인간의 취약함과 판단·협력을 느끼게 해야 한다.
- 역사적 사실 `[교과서]`, 사실과 충돌하지 않는 가상 상황 `[재구성]`, 상호작용 장치 `[게임]`을 구분한다.
- 이동 생활은 처음부터 정답으로 설명하지 않고 세 역할의 경험이 누적되어 도달하는 결론으로 만든다.
- 새 기능은 교육적·체험적 의미가 있을 때만 추가하며 같은 경험을 더 단순하게 구현할 수 있는지 먼저 검토한다.

## 4. 몰입 Guardrail

모든 새 역할/장면은 다음을 확인한다.

- **상황**: 학생은 어디에 있는가?
- **몸**: 무엇을 보고/듣고/느끼는가?
- **관계**: 누구와 함께 있고 누가 기다리는가?
- **욕구**: 지금 왜 행동해야 하는가?
- **행동**: 학생이 직접 무엇을 하는가?
- **반응**: 선택 뒤 세계·사람·시간 중 무엇이 바뀌는가?
- **연속성**: 앞의 장소·말·단서가 뒤에서 이어지는가?
- **회수**: 앞서 심은 감정/모티프가 뒤에서 의미 있게 돌아오는가?

기능이 작동해도 이 항목이 무너지면 Vertical Slice를 완성으로 간주하지 않는다.

## 5. ChatGPT 채팅 중심 개발 원칙

- 개발·기획·QA·아트 디렉션은 작업 책임 단위별 새 ChatGPT 세션으로 나눈다.
- 한 책임이 끝나면 세션을 종료한다. 대화 길이를 기준으로 하지 않는다.
- 한 세션에서 범위 밖 기능까지 임의로 확장하지 않는다.
- 새 세션은 저장소 최신 상태를 읽고 시작한다.
- GitHub 최신 코드가 빌드 기준이며 별도 `LATEST_BUILD.zip`을 사용하지 않는다.
- 중요한 사용자 플레이 피드백은 HUX ID 등으로 GitHub에 남긴다.

## 6. 이미지·사운드 원칙

일반 기획·개발 세션에서는 최종 이미지를 생성하지 않는다.

- 외부 이미지를 임의로 최종 자산으로 사용하지 않는다.
- 필요한 자산은 `handoff/ASSET_REQUESTS.md`에 기록한다.
- 최종 아트/사운드는 기능과 몰입 구조가 검증된 뒤 제작한다.
- 단, **사운드·시각이 어떤 서사 기능을 맡는지는 기획 단계부터 설계**한다.
- 이미지는 `예쁜 배경`보다 위치·시간·관계·행동 정보를 전달해야 한다.
- 사운드는 공간감, 행동 피드백, 관계 대사, 위험 신호를 우선하고 음악은 보조한다.
- 최종 제작 전 `VISUAL_CONTEXT_BIBLE`, `VISUAL_CONTINUITY_MAP`, `ART_DIRECTION_BIBLE`, `ASSET_SPEC`을 만든다.
- 승인 자산만 `ASSET_MANIFEST`에 등록한다.

## 7. 테스트 원칙

자동 테스트는 기능·아키텍처 Guardrail을 검증한다.

교사/학생 플레이테스트는 다음을 별도로 검증한다.

- 역할 빙의
- 이해도와 조작
- 텍스트 부담
- 선택의 결과감
- 관계와 공동체 존재감
- 시간·공간 연속성
- 감정 회수
- 역사적 의미

자동 테스트 통과만으로 체험 완성을 선언하지 않는다.

## 8. 저장소 변경 세션 종료 시 갱신

필요한 항목을 갱신한다.

- `PROJECT_STATUS.md`
- `CHANGELOG.md`
- `handoff/CURRENT_HANDOFF.md`
- 테스트 시 `handoff/TEST_REPORT.md`
- 미해결 문제 시 `handoff/KNOWN_ISSUES.md`
- 자산 필요 시 `handoff/ASSET_REQUESTS.md`

`CURRENT_HANDOFF.md`는 다음 새 세션이 과거 대화를 몰라도 시작할 수 있을 정도로 작성한다.

## 9. 전체 개발 계획

단계·입력·품질 게이트는 `docs/00_DEVELOPMENT_WORKFLOW.md`를 따른다.
