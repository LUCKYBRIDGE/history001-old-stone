# AGENTS.md

이 저장소는 초등학생용 **구석기 역사 체험 웹게임** 프로젝트의 단일 기준점(Single Source of Truth)이다.

## 1. 모든 새 ChatGPT 작업 세션의 시작 순서

작업을 시작하기 전에 반드시 다음 순서로 읽는다.

1. `AGENTS.md`
2. `PROJECT_STATUS.md`
3. `docs/00_DEVELOPMENT_WORKFLOW.md`
4. `handoff/CURRENT_HANDOFF.md`
5. 이번 작업과 직접 관련된 `docs/` 문서

과거 채팅의 기억보다 저장소의 최신 문서를 우선한다.

## 2. 문서 위계

프로젝트 기획 문서가 추가된 뒤에는 아래 우선순위를 따른다.

1. `docs/01_PROJECT_CORE.md` — 최상위 교육·체험 원칙
2. `docs/02_EXPERIENCE_STRUCTURE.md` — 전체 경험 구조
3. `docs/05_ROLE_EXPERIENCE_MAP.md` — 세 역할의 경계와 균형
4. 각 역할의 `*_STORY.md`
5. 각 역할의 `*_PLAYFLOW.md`
6. 기술·구현 문서

하위 문서나 기존 코드가 상위 원칙과 충돌하면 임의로 정당화하지 말고 충돌을 명시한다.

## 3. 프로젝트 핵심 원칙

- 학생에게 구석기 시대를 설명하는 웹이 아니라, 잠시 그 생활 속에서 살아보게 하는 웹게임을 만든다.
- 사냥·채집·머무는 사람은 주역/보조역 관계가 아니라 같은 공동체의 하루를 보여주는 동등한 세 관점이다.
- 먼저 개발된 역할의 플레이 문법을 다른 역할에 자동 복제하지 않는다.
- 프로젝트의 핵심 단위는 세 개의 미니게임이 아니라 **하나의 공동체가 보내는 하루**다.
- 실패는 `GAME OVER`가 아니라 당시 생활의 불확실성을 경험하는 정상적인 결과다.
- 점수, HP, EXP, 랭킹, 전투 중심 구조를 임의로 추가하지 않는다.
- 위험은 공포·전투의 쾌감보다 자연 속 인간의 취약함과 판단·협력을 느끼게 해야 한다.
- 역사적 사실 `[교과서]`, 체험을 위한 가상 상황 `[재구성]`, 상호작용 장치 `[게임]`을 구분한다.
- 이동 생활은 처음부터 정답으로 설명하지 않고 세 역할의 경험이 누적되어 도달하는 결론으로 만든다.
- 새 기능은 교육적·체험적 의미가 있을 때만 추가하며, 같은 경험을 더 단순하게 구현할 수 있는지 먼저 검토한다.

## 4. ChatGPT 채팅 중심 개발 원칙

- 개발·기획·QA·아트 디렉션은 **작업 단위별 새 ChatGPT 채팅 세션**으로 나눈다.
- 세션이 길어져서 바꾸는 것이 아니라 **한 책임이 끝나면 세션을 종료한다.**
- 한 세션에서 범위 밖 기능이나 다음 단계까지 임의로 확장하지 않는다.
- 새 세션은 과거 대화를 전제로 하지 않고 저장소 문서와 현재 코드를 읽고 시작한다.
- 실제 코드가 생긴 뒤에는 GitHub의 최신 코드가 빌드의 기준이며 별도의 `LATEST_BUILD.zip`을 세션 사이에 전달하지 않는다.

## 5. 이미지·사운드 원칙

일반 기획·개발 세션에서는 이미지를 생성하지 않는다.

- 개발 세션에서 이미지 생성 금지
- 외부 이미지를 임의 검색·다운로드하여 최종 자산으로 사용 금지
- 필요한 자산은 `handoff/ASSET_REQUESTS.md`에 기록
- 최종 이미지는 기능이 검증된 뒤 별도 이미지 제작 공정에서 만든다.
- 이미지 제작 전 `VISUAL_CONTEXT_BIBLE`, `VISUAL_CONTINUITY_MAP`, `ART_DIRECTION_BIBLE`, `ASSET_SPEC`을 통해 장면 맥락과 연속성을 전달한다.
- 이미지 제작 도구는 ChatGPT 이미지 생성, Antigravity 등 프로젝트에서 선택한 도구를 사용할 수 있다.
- 승인된 자산만 `ASSET_MANIFEST`에 등록하고 앱에 연결한다.
- Google 이미지 등은 기본적으로 시각·역사 레퍼런스 조사 용도로 사용하며 최종 자산 사용 시 권리를 별도로 확인한다.

## 6. 세션 종료 시 갱신할 것

저장소에 실제 변경을 한 세션은 작업 종료 전에 필요한 항목을 갱신한다.

- `PROJECT_STATUS.md`
- `CHANGELOG.md`
- `handoff/CURRENT_HANDOFF.md`
- 테스트를 수행했다면 `handoff/TEST_REPORT.md`
- 미해결 문제가 생겼다면 `handoff/KNOWN_ISSUES.md`
- 이미지·사운드가 필요해졌다면 `handoff/ASSET_REQUESTS.md`

`CURRENT_HANDOFF.md`는 다음 새 ChatGPT 세션이 과거 대화를 몰라도 작업을 시작할 수 있을 정도로 작성한다.

## 7. 현재 전체 개발 계획

세부 단계와 각 단계의 입력·도구·결과물은 `docs/00_DEVELOPMENT_WORKFLOW.md`를 따른다.
